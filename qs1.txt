. Take a number and print whether it’s positive, negative, or zero

// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
int num;

printf("Enter a Number");
scanf("%d",&num);

if(num==0)
{
    printf("Number Is Zero %d",num);
}
if(num>0)
{
        printf("Number is Positive %d",num);
}
if(num<0)
{
        printf("Number is Nagetive %d",num);

}
    return 0;
}
